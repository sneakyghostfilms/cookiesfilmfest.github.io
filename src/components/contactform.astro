---
---
<section class="max-w-4xl mx-auto p-6 bg-transparent shadow-md rounded-md">
    <form id="contact-form" action="https://formspree.io/f/meqyyzag" method="POST" class="space-y-4" onsubmit="return handleFormSubmit(event);">
      <div>
        <input type="text" id="name" name="name" placeholder="Name" class="mt-1 block w-full px-3 py-2 bg-transparent border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm text-white placeholder-white" required>
      </div>
      <div>
        <input type="email" id="email" name="_replyto" placeholder="Email" class="mt-1 block w-full px-3 py-2 bg-transparent border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm text-white placeholder-white" required>
      </div>
      <div>
        <input type="text" id="phone" name="phone" placeholder="Phone Number (optional)" class="mt-1 block w-full px-3 py-2 bg-transparent border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm text-white placeholder-white">
      </div>
      <div>
        <textarea id="message" name="message" rows="4" placeholder="Message" class="mt-1 block w-full px-3 py-2 bg-transparent border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm text-white placeholder-white" required></textarea>
      </div>
      <div class="flex justify-center">
        <button type="submit" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
          Send Message
        </button>
      </div>
    </form>
    <div id="thank-you-message" class="hidden text-center text-white">
      <p>Thank you for reaching out! We will get back to you shortly.</p>
    </div>
  </section>
  
  <script>
    function handleFormSubmit(event) {
      event.preventDefault();
      const form = event.target;
      const data = new FormData(form);
  
      fetch(form.action, {
        method: form.method,
        body: data,
        headers: {
          'Accept': 'application/json'
        }
      }).then(response => {
        if (response.ok) {
          form.reset();
          document.getElementById('thank-you-message').classList.remove('hidden');
        } else {
          alert('There was a problem submitting your form. Please try again.');
        }
      }).catch(error => {
        alert('There was a problem submitting your form. Please try again.');
      });
    }
  </script>
  